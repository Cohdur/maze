

/*
struct Compare {
    bool operator()(const std::pair<int, Vertex>& a, const std::pair<int, Vertex>& b) {
        return a.first < b.first; // Max-Heap: Higher priority comes first
    }
};

//std::priority_queue<std::pair<int, Vertex>, std::vector<std::pair<int, Vertex>>, Compare> maxHeap; // set end to INT_MAX
*/